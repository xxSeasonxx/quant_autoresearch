# Bounded params for the active strategy. Every entry here is read by strategy.py;
# upstream owns book scale, so there is no magnitude/scale knob to search over.

[params]
top_n = 5
min_abs_funding_bps = 1.0
decision_lag_minutes = 1

[bounds.top_n]
min = 5
max = 5

[bounds.min_abs_funding_bps]
min = 1.0
max = 1.0

[bounds.decision_lag_minutes]
min = 1
max = 1
