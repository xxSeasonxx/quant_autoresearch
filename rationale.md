# Rationale

**Blank bench.** No active thesis. The previous thesis was offloaded — see `OFFLOADED.md`.

This file is the thesis document for the *active* lifecycle: mechanism, observable, falsifier,
assumptions, editable params, the first failure mode to watch, the Lever Enumeration, and the
per-attempt log. `new-thesis-setup` writes it at setup; `program.md` governs it during the loop.

## Next thesis

Not chosen. Run `new-thesis-setup` to fit a mandate, propose a `protocol.toml`, get Season's approval,
and establish the first baseline. Do not hand-write a thesis here — setup owns the protocol contract,
the universe resolution, and the lock.
